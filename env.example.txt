# ─────────────────────────────────────────────────────────────
# GoalScope Backend — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────

# Your API-Football key from dashboard.api-football.com
# Pro plan ($19/mo) gives 7,500 req/day — enough for ~2,000 user searches/day
API_FOOTBALL_KEY=YOUR_API_FOOTBALL_KEY_HERE

# Port the server listens on (Render sets this automatically)
PORT=3000

# Allowed frontend origins — comma-separated (no trailing slash)
# Example: https://goalscope.onrender.com,https://yourdomain.com
# Use * during local development only
ALLOWED_ORIGINS=*

# Cache TTL in seconds (reduces API calls for repeated searches)
# 300 = 5 minutes. Increase to 600+ in production.
CACHE_TTL=300
